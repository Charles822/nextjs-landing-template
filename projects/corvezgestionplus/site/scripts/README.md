# Image Generation

Use the canonical script in landerMachine:

```bash
cd ../../../../  # to landerMachine root
python scripts/generate-image.py "prompt" output_name --slug corvezgestionplus
```

See `landerMachine/AGENT_INTEGRATION.md` for full usage. Requires `OPENROUTER_API_KEY`.
